Show what changed in the references and tools since the last notable update.

Run: `git log --oneline -20 -- references/ app/api/claude/route.ts app/chat/page.tsx`
Then summarise: new tools added, new API endpoints discovered, rule changes, architecture updates.
